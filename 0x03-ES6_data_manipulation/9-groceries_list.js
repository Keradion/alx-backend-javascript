export default function groceriesList() {
  // Creating a new Map
  const list = new Map();

  // Adding keys and values
  list.set('Apples', 10);
  list.set('Tomatoes', 10);
  list.set('Pasta', 1);
  list.set('Rice', 1);
  list.set('Banana', 5);

  return list;
}
